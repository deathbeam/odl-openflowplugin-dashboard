# openflowplugin-dashboard

`moustache` is required to build.
To install `moustache`, first install `npm` and then run

```shell
sudo npm install -g moustache
```

Now, to build dashboards, run `make` in this directory.
